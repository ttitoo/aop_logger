-# 胡腾原话: 这里有个spa，调试用的，但是暂时没放出来用。
-# 所以访问 /logging 路径会看到一个 "添加" 按钮，除此之外什么都没有。
#logging-context
-# 一个 React 项目会挂载到这个元素上。
%link{ rel: :stylesheet, href: "https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" }
%link{ rel: :stylesheet, href: "https://cdn.jsdelivr.net/npm/sweetalert2@11.6.7/dist/sweetalert2.min.css" }

- MiraclePlus::Logger::ScriptsManager.assets.each do |asset|
  - if asset.end_with?('js')
    %script{ src: "/assets/#{asset}" }
  - else
    %link{ rel: :stylesheet, href: "/assets/#{asset}" }
